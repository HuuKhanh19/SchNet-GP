# CONAN-SchNet Project Requirements
# conda env: conan_es (Python 3.10, PyTorch 2.8.0+cu129)

# Core ML
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
numba>=0.57.0          # conformer.py: floyd_warshall (@njit)

# Graph NN — model uses torch_geometric.nn.radius_graph / MessagePassing
# NOTE: PyG 2.8 routes radius_graph through pyg-lib>=0.6.0 (no mac-arm64 wheel).
#       For a CPU/Mac env, pin torch_geometric<2.6 and install torch_cluster so
#       radius_graph falls back to torch_cluster. On the GPU server, a PyG build
#       with a working radius backend (pyg-lib or torch_cluster) is required.
torch_geometric>=2.5,<2.6
torch_cluster        # provides radius_graph backend for torch_geometric<2.6

# Molecular
schnetpack>=2.0.0
rdkit>=2023.03.1
ase>=3.22.0

# Config & logging
hydra-core>=1.3.0      # scripts/run_step1.py entrypoint
omegaconf>=2.3.0
pyyaml>=6.0
tensorboard>=2.14.0
tqdm>=4.65.0

# Plotting
matplotlib>=3.7.0

# GP (already installed)
# evogp==0.1.0
